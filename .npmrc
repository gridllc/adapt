/*
# Only show high or critical vulnerabilities (silence moderate noise like undici)
audit-level=high

# Disable funding messages in console output
fund=false

# Prevent peer dependency errors in legacy packages
legacy-peer-deps=true

# Always save exact versions in package.json (no ^ or ~)
save-exact=true
*/

// This file appears to contain configuration for .npmrc.
// It has been commented out to resolve build errors.
// You should move this content to a file named ".npmrc" in the root of your project and then delete this file.

export {};
